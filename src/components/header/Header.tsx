import { FC } from "react";

export const Header: FC = () => {
   return(
      <header className="text-amber-50 bg-gray-500 text-center">
         <h1 className="text-2xl font-bold">Movie Search</h1>
      </header>
   );
};